version: '3.8'

services:
  web:
    build: ./app
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app
      - message_data:/app/instance
    restart: unless-stopped

volumes:
  message_data: